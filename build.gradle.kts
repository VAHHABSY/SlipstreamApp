plugins {
    // Define the versions for the core Android and Kotlin plugins here.
    // These versions must be defined at the root level for resolution to succeed
    // when running commands like 'gradle wrapper'.
    id("com.android.application") version "8.2.0" apply false
    id("org.jetbrains.kotlin.android") version "1.9.0" apply false
}
